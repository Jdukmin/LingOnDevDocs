{
  "component": "frontend",
  "version": "0.2.0",
  "last_updated": "2026-07-31",
  "changelog": "changelog/frontend.md",
  "source_repo": "jdukmin/letmeknow",
  "source_commit": "ad0e0f4 (+ b0923f0, docs-submodule-pointer-only, no additional source change)",
  "known_discrepancy": [
    "jdukmin/letmeknow's own pubspec.yaml still says \"version: 1.0.0+1\" as of commit ad0e0f4 — a placeholder/default Flutter version, not tracking this DevDocs-side baseline (and numerically ahead of it, which would be misleading if read in isolation). This 0.2.0 value is the DevDocs SSOT's authoritative version per CLAUDE.md's Version 관리 rule; pubspec.yaml should be reconciled in a future frontend-repo pass (out of scope here — DevDocs makes no source-code changes).",
    "2026-07-31 Documentation Synchronization pass found that jdukmin/letmeknow's local working tree (NOT committed — `git diff HEAD` confirmed all 7 dashboard widget files differ substantially from commit ad0e0f4) now has all 7 dashboard widgets actually calling SmallCard/MediumCard/LargeCard, contradicting this file's prior note that \"Card Component System... 실제로 채택한 위젯이 아직 하나도 없음\". That prior note remains accurate against the committed ad0e0f4 — the discrepancy is against the uncommitted local state only. version was deliberately NOT bumped for this specific finding — Version 관리 rule requires actual committed implementation, and uncommitted working-tree code doesn't qualify. Re-verify and bump once (if) this lands in a real commit."
  ],
  "note": "V_0.2.0: Minor bump — Theme Token 변경(Green Primary/Secondary/Accent 확정) + Component Contract 추가(Card System) + Frontend Design System 확장(Typography card-content tier, Widget Presentation Rule), 세 조건 모두 충족. 이전 상태(V_0.1.0, 28f8bb2)에서 프론트엔드 소스가 커밋 ad0e0f4(\"updated UI modules\")까지 앞서 나간 것을 이번 패스에서 코드 직접 대조로 재확인·반영했다 — aod_colors.dart/aod_theme.dart/app_card.dart/app_typography.dart/app_spacing.dart 및 7개 대시보드 위젯 파일의 `WidgetVariant` switch 분기를 직접 읽어 확인(grep으로 전수 확인, 목록은 changelog 참고). `flutter analyze`/`flutter test`는 이 DevDocs 패스에서 실행하지 않았다 — 소스 코드 정적 대조만 수행했다(Mock 결과 아님, 코드 자체를 읽은 결과). Card Component System(SmallCard/MediumCard/LargeCard)은 커밋 ad0e0f4 기준으로는 코드상 존재하나 실제로 채택한 위젯이 없다 — 단, 2026-07-31 재확인 결과 로컬 uncommitted working tree에는 7개 위젯 전부의 채택 코드가 있음(커밋 안 됨, known_discrepancy 참고). domain_icd/dashboard.md·dashboard_requirements.md는 이번 패스에서 변경하지 않았다(WidgetVariant/LayoutConstraint/LayoutResolver의 Domain ICD 정식화는 별도 패스 필요 — changelog 참고). Prior baseline: V_0.1.0, confirmed real via FRONTEND_VERIFICATION_REPORT.md + FRONTEND_SCHEMA_VERIFICATION_REPORT.md — reconciled against jdukmin/letmeknow commit 28f8bb2. See status/frontend/V0.1.0.md for that baseline's full detail."
}
